# BitGo-Takehome


The application:
- Runs on port 8080
- Exposes endpoints: `/` (hello world), `/health`, `/metrics` (Prometheus format)
- Includes metrics for request count, latency, memory usage, and CPU time
- Perfect for testing auto-scaling policies
